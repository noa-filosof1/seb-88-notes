# SEB-88 class/lab notes

- This directory will have all up to date lesson/lab notes from class. 
- This will not include any `node_modules`. Should you wish to run this code you will need to run an `npm install` in the specific directory you are working in to install all the dependencies.